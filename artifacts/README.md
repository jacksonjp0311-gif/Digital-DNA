# Artifacts

Runtime and verification outputs are persisted here.

## Mini Directory
- `last_run.json` — canonical output from most recent DDNA execution.
- `reports/` — archived self-verification and report artifacts.

## Interlinking
- Produced by `engine/orchestrator` and tests.
- Intended as machine-readable contract outputs for external systems.
